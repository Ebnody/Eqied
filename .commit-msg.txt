feat(support): floating help bubble for users to report issues + ticket thread API (Phase 3)

- New schema: IssueReport + IssueMessage models (subject, category, status, threaded messages)
- New API: POST/GET /api/issues, GET /api/issues/[id], POST /api/issues/[id]/messages
- New SupportBubble client component (bottom-right floating button on user pages)
  - Lists user's tickets with unread badge and status icons
  - New ticket form (subject, category, body)
  - Thread view with reply box (closed tickets are read-only)
  - Polls /api/issues every 60s for unread admin replies
- Mounted on (dashboard) and (roommate) layouts
- User reopen behavior: replying to a resolved ticket reopens it
