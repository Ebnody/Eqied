feat(admin): real notification bell with dropdown of recent events (Phase 5)

- New API: GET /api/admin/notifications (combines unread tickets + recent signups + new groups)
- New NotificationBell client component with dropdown panel
- Polls every 30s; red badge shows unread ticket count (capped at 9+)
- Click any notification -> navigates to relevant page
- "View all messages" footer link to /admin/messages
- Messages page now reads ?id= query param to auto-open a ticket from notification links
- Replaces the static "3" demo badge in admin header
