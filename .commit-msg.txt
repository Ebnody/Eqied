feat(admin): support inbox at /admin/messages with thread view + reply (Phase 4)

- New API: GET /api/admin/issues (list with status/search filters)
- New API: GET /api/admin/issues/unread-count (sidebar badge polling)
- New API: GET/PATCH /api/admin/issues/[id] (fetch + status changes)
- New API: POST /api/admin/issues/[id]/messages (admin reply, auto-promotes open->in_progress)
- New page /admin/messages with split list/thread layout
- Status filters (All / Open / In Progress / Resolved / Closed)
- Search by subject
- Per-message bubble UI with admin replies in green
- Inline status change buttons in thread header
- Sidebar nav now has Messages item with red unread badge polling every 30s
- All admin replies and status changes logged to AdminLog
